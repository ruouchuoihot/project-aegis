# Config

This directory holds support configuration that is useful for bootstrapping or repairing the lab, but is not part of the main runtime path.

- `payloads/`: small JSON request bodies used with Kibana, Elasticsearch, and Fleet APIs during setup/debugging.

